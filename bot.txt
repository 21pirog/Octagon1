const TelegramBot = require('node-telegram-bot-api');

// replace the value below with the Telegram token you receive from @BotFather
const token = '7185067535:AAEEqZmL_MLShQmZMbP46DzQQaAbCrNVvfk';

// Create a bot that uses 'polling' to fetch new updates
const bot = new TelegramBot(token, {polling: true});

// Matches "/echo [whatever]"
	bot.onText(/start/, function (msg, match) {
	var userId = msg.from.id;
	bot.sendMessage(userId, 'Привет, Октагон!');
});

 bot.onText(/help/, function (msg, match) {
	var userId = msg.from.id;
	bot.sendMessage(userId, '/site - отправляет в чат ссылку на сайт октагона \n /creator - отправляет в чат ваше ФИО');
});

	bot.onText(/site/, function (msg, match) {
	var userId = msg.from.id;
	bot.sendMessage(userId, 'https://students.forus.ru');
});

	bot.onText(/creator/, function (msg, match) {
	var userId = msg.from.id;
	bot.sendMessage(userId, 'Куличкина Алина');
});